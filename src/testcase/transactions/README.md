# FAB Transactions Testcases

Copy the `adpay`, `adpay_icp`, `mpay`, and `mrchptl` folders into `src/testcase/transactions/`.
The testcases use the existing New Framework `TransactionsPage`, `ConfigManager`, and `ExcelUtil`. Bank is resolved from the framework configuration.
